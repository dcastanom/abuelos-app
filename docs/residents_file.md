## story's requirements
- based on the Collection: `residents` in the design.md file
- It's already curated based on the file docs/FICHA GERONTOLOGICA.docx definitions and adjusted as needed. No nede to review the file docs/FICHA GERONTOLOGICA.docx again.
- yes or no questions require option boxes 
- several options questions require checkboxes (allow several selected), almost all of them have a OTRA or OTRAS options. When selected the user must specify which one or ones.
- The photo field must be a modern file loading field. 2 MB max. Allowing to be edited and create a thumbnail after uploaded.
- The list page must allow searching by name with an autocomplete text field.
- The list must show the thumbnail of the resident's photo as its first field in the table.
- The detailed view of the resident can be exported to pdf.